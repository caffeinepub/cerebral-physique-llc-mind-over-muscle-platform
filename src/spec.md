# Specification

## Summary
**Goal:** Trigger a rebuild by making a minimal, one-character text change on the Contact page.

**Planned changes:**
- Add exactly one `.` character to existing visible text in `frontend/src/pages/ContactPage.tsx` without changing layout, styling, routing, or functionality.

**User-visible outcome:** The Contact page displays the same content as before, with one additional period in the existing visible text.
